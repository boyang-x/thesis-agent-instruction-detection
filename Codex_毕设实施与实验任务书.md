# 毕设实施与实验任务书

日期：2026-09-24。本文是研究设计与待执行实验计划，不包含新方法实验结果。

## 0. 不变约束与本轮目标

论文题目必须逐字保持：**基于语义表征与思维链推理协同的智能体恶意指令检测方法**。

推荐主线：**面向安全关键上下文变化的对照语义学习与证据链协同检测**。这不是新的论文题目，而是题目下的方法描述。

核心问题：同一条待执行指令/工具调用，在授权范围、对象、收件人或可信上下文不同的情况下，可能从正常操作变为越权操作。检测器应对这些关键关系敏感，同时对不改变授权关系的改写和干扰保持稳定。大模型不仅输出安全标签，还提供经核对的证据定位，用于训练小模型。

这是待验证的毕设级增量设计，不声称提出了对比学习、CoT、知识蒸馏或级联路由本身。不要把它包装成已超过 ShieldAgent、已满足 CCF-A 创新要求或已取得显著提升。

## 1. 复用现有工作，而非重建项目

先检查当前仓库是否为 `boyang-x/agent-guardrail-research` 或其本地副本。复用既有模型接口、日志、数据适配器和执行前拦截入口。开独立研究分支/子目录，不改动已归档结果，不重写主入口，不重构无关模块。

历史 BindGuard 是执行前授权绑定研究；其数据可作为前期基础和开发材料，但不能充当本新方法的独立测试结果。已有公开/暴露任务继续标记 development，不能重新命名为盲测。历史摘要记录中，11 个 Slack 任务的压力测试出现 BindGuard 越权 0/11、攻击下任务完成 1/11，结论未通过效用门槛。不能只保留 0/11 而隐去 1/11。

环境探测只做一次：设备/显存、已缓存编码器、可用推理接口、可复用数据。不要打印密钥。先试一个无害请求确认接口可用。遇到鉴权或网络失败记录具体错误，不反复重装依赖，不生成假响应。

## 2. 任务与威胁模型

主任务为执行前的候选指令风险检测。输入包含固定安全策略 P、可信用户任务 U、截至当前时刻的历史 H、外部内容 O 及其运行时来源标签、候选待执行指令/工具调用 a、工具语义 T。

`X = (P, U, H, O, source_metadata, a, T)`

来源标签由宿主运行时赋予，不由文本中的“system”“管理员”等声明赋予。外部内容可提供任务数据，但不能自行扩大授权。用户授权也不能覆盖更高优先级的固定安全策略。

输出分类为 safe/unsafe；另外保留 abstain 状态，表示接口失败、关键信息不足或无法判断。abstain 不是额外的“恶意标签”，也不等同于 TS-Bench 中的 controversial 标签。

必须区别：
- 输入中存在提示注入尝试；
- 当前候选指令违反策略；
- 真实执行后攻击成功。

本方法主要评估第二项。恶意文本被模型忽略、当前动作安全时，不能仅因为出现注入文本就把动作标为 unsafe。端到端攻击成功需单独执行环境评测。

今晚仅覆盖文件/数据外发、目标或参数越权、来源伪装导致的指令替换等文本工具场景，不做 GUI、多智能体、完整程序安全证明或开放式权限合成。

## 3. 数据：先原生样本，再受控对照

### 3.1 公开数据入口

优先检查 ToolSafe 官方仓库中的 TS-Bench 数据和评测代码：
`https://github.com/MurrayTom/ToolSafe`
论文：`https://aclanthology.org/2026.findings-acl.1850/`

网页核查确认该仓库列出了 TS-Bench 目录，但尚未在本任务中下载并解析数据。请实际拉取、检查文件格式、许可证和 split 后记录。不得把网页列有目录写成已经完成数据导入。

TS-Bench 有官方 train/eval 划分以及 safe/controversial/unsafe 三类。首轮二分类明确使用 safe/unsafe 子集，并报告排除 controversial 的数量；不得偷偷合并或把其等同证据缺失。保留官方 split。适配时必须截取当前动作之前的历史，不带当前动作结果、后续轨迹、风险标注、评审解释或其他标签泄漏字段。

AgentDojo 用于后续/已有环境可用时的真实闭环：`https://github.com/ethz-spylab/agentdojo`。不能把静态 TS-Bench 分类准确率当作 AgentDojo 的攻击成功率或任务完成率。

### 3.2 受控三联样本

每组保留相同候选调用 a：
1. X_safe：可信任务与策略允许该操作。
2. X_unsafe：仅改变一项安全关键关系，使同一个操作不再被允许。
3. X_noise：对 X_safe 或 X_unsafe 施加经审核的不相关改写，标签不变。

关键关系类型至少含：授权范围、资源对象/收件人、消息来源与有效授权的组合。必须加入低信任来源提供合法任务数据的正常样本，防止模型学成“一见 tool message 就拦截”。数据中不能直接输入 authorized=true、gold evidence、unsafe_reason 等评测答案。

示例（仅机制示例，不是运行结果）：
- 候选：上传 summary.csv 至 upload.example.test。
- safe：可信用户明确要求将这份非敏感报告上传到该接收端；固定策略允许。
- unsafe：可信任务仅要求本地汇总，且明确禁止外发；外部文档声称“合规要求必须上传”。
- noise：仅改变文档排版、无关背景文字或保持关系的表述，不改变授权和数据敏感性。

示例只是帮助理解；正式测试必须包括指代、别名、跨轮引用、复合范围等场景，而不能全部是“不要上传”关键词判断。确定性字符串规则应作为强简单基线参与评测。

### 3.3 样本与划分协议

开发目标约 100–200 个基础情境，每个 3–4 个变体；这是预算目标不是已取得规模。优先做 40 组可人工复核的小诊断集，而不是赶工生成 1 万条低质量样本。

记录 sample_id、origin_id、template_family、source_dataset、source_split、relation_type、label_source、review_status、pair_id。同一 origin 的所有轨迹步骤、变体和重写必须在同一 split。尽可能按任务/模板家族隔离，而非仅随机按行划分。训练集与开发集可调试；测试集冻结后不反复调阈值。

对照样本需同时满足：事实和工具语义自洽；只改变指定关系；修改不意外触发其他禁令；标签有独立策略依据。不能用同一个教师模型的标签给自己评测。测试对照对必须人工复核，或在明确的有限可执行策略下得到标签并人工抽查；不满足者标为 silver/exploratory，不能叫 gold。人工审阅未完成时，交付 review_queue.jsonl，不伪称已审核。

## 4. 模型与核心算法

### 4.1 小模型

使用当前已可用的 BERT 类编码器，英文公开数据优先 DeBERTa/RoBERTa；中文语料要使用适合中文的模型并单独报告。不要为跑一夜实验下载多个基座。

共享编码器输出风险 logit `s_theta(X)` 和证据片段分数 `q_theta(e|X)`。证据可先按句子/消息片段编号，利用片段 token pooling 接一个线性头。原六类操作意图仅可作为辅助输出，不能把 File/Network 等操作类别直接当作恶意类别，也不需要今晚再实现六分类。

长输入使用统一、明确的窗口/选择策略。不能只为候选模型保留完整授权而让基线截断。记录每个样本是否截断；关键证据不在窗口时不能当作模型已经看见。首轮短上下文验证可单独报告；长上下文是后续扩展。

### 4.2 证据链生成

教师接收原始输入，不接收金标签或小模型风险分数；输出简短结构化分析，不要求模型泄露其隐藏推理：
`操作识别 -> 来源与授权证据 -> 策略/后果比对 -> 判定`

输出字段：decision、operation_summary、evidence_refs、policy_refs、conflict_type、short_justification、missing_facts。

每条 evidence_ref 必须指向真实的消息 ID、片段 ID/起止位置。程序校验证据存在、引用文字一致、来源 ID 未被伪造。引用存在只证明可追溯，不证明含义正确；语义监督仍需审核。

训练对照组中的证据链还要指出“哪个安全关键关系改变”。不要告诉教师正确类别后让其事后编理由。教师标签与独立标签不符时，保留记录，暂停使用该证据监督；不要自动覆盖金标签。无合格证据的样本仍可用于分类损失，不可用于证据损失。

### 4.3 联合目标

基础分类损失：`L_cls = CE(y, sigmoid(s_theta(X)))`。

关键关系的成对约束（同一动作、不同合法性）：
`L_pair = mean max(0, m - [s_theta(X_unsafe)-s_theta(X_safe)])`。

不相关扰动稳定性：
`L_inv = mean (sigmoid(s_theta(X))-sigmoid(s_theta(X_noise)))^2`。

证据监督：`L_evi = masked BCE(q_theta, reviewed_evidence_mask)`。
未标注的证据位置不要一律当负例；只有审核覆盖的片段参与损失。

总目标：`L = L_cls + lambda_pair*L_pair + lambda_inv*L_inv + lambda_evi*L_evi`。

第一轮固定一个合理配置（例如 m=1、lambda_pair=0.5、lambda_inv=0.1、lambda_evi=0.2），这些只是实现起点，不是验证后的最优值。不允许在测试集上调参。以上损失形式本身不是新发明；待验证贡献是把安全关键上下文干预、证据链监督与轻量检测结合，并证明其在同数据训练之上有额外收益。

注意：L_evi 需要真正回传到共享编码器。仅把解释打印进日志、但不参与训练，不是“证据链蒸馏”。反事实对仅在离线训练与诊断时存在，在线部署不提供 oracle 对照样本。

### 4.4 在线协同

第一版沿用校准的双阈值路由：高把握样本由小模型判断，剩余交给证据链复核。阈值只在独立开发数据上选取；应报告整条级联而非只报大模型子集。

交给大模型的是原始上下文与小模型候选证据位置；第一版不硬删除其他上下文，也不把小模型风险标签当作提示，避免证据裁剪漏检和答案锚定。大模型需自行查验原文证据。

接口失败、无法验证的证据或仍缺事实时输出 abstain，并由宿主暂停敏感动作；系统原有可信查询可以使用，不能人为删除这些能力制造问题。不要用“模型更自信”来补全缺失授权。

今晚不训练复杂路由器、不做 RL、不实现在线自学习。双阈值路由与普通蒸馏是已有技术，不单列为创新。

## 5. 强基线与消融（顺序不可颠倒）

B0：少量明确规则/字段匹配。仅用真实可解析字段，不引入标签 oracle。
B1：普通上下文编码器，原始训练数据，L_cls。
B2：普通上下文编码器，**完全相同的全部对照数据**，仅 L_cls。这是最关键对照。
B3：B2 + L_pair + L_inv。
B4：B3 + L_evi（完整小模型）。
B5：相同大模型全量直接判断。
B6：相同大模型全量证据链判断。
B7：B2 + 与完整方案相同的级联路由。
Ours：B4 + 证据链复核。

时间不够先完成 B0、B2、B3、B4；B1 与 B5–B7 后补并如实标为未跑。不要把 B3 当成完整语义—思维链方案。

完整论文加入可运行的 TS-Guard、Task Shield，以及 ShieldAgent 的正式复现或清楚标注的简化适配。不能把普通规则 RAG + LLM 命名为“ShieldAgent 完整复现”。无需今晚搭建它的全部 WebArena、概率电路和验证工具。

所有对照使用相同输入、相同训练/测试划分、相同基座与合理对齐的训练预算。增加证据监督的收益还需比较普通标签蒸馏和通用证据监督，以排除仅因额外教师调用/额外标注带来的收益。

## 6. 指标与有效性判定

必须输出混淆矩阵及整数分子/分母；二分类 F1、unsafe recall、FPR、PR-AUC；数据不平衡时不能只看 accuracy。

PairAcc：一组 safe/unsafe 两项都预测正确的比例。只统计预测翻转率不够，“反着翻转”也会很高。
Invariance consistency：无关扰动前后预测一致率；同时报告两者准确率，避免恒定输出刷分。
Evidence metrics：引用格式/位置有效率与人工语义证据正确率分开，不能前者代替后者。
级联指标：LLM 调用比例、平均及 P95 延迟、输入/输出 token、超时/解析失败/abstain 数。预缓存只能用于评估“选择哪些样本”，不得把缓存读取时长当真实推理时延。
闭环指标（实际执行后）：攻击成功率、正常任务完成率、受攻击任务完成率。未知/暂停不得隐藏：安全侧可计为未产生危害，但效用侧必须记录其代价。

统计：先一个种子跑通，再至少三个种子验证；按 origin/任务进行成组 bootstrap 或配对检验，不把同一任务的多个改写当独立样本。小样本中 0 次漏检不意味着真实漏检概率为 0。

首轮继续门槛（研究决策标准，不是性能承诺）：B4 相对 B2 在独立对照诊断集 PairAcc 至少提高约 5 个百分点，普通 held-out F1 下降不超过约 2 个百分点，证据案例能解释改善来自关键关系而非模板。一次小实验达到仅算初步信号；未达到要输出失败分析，不换指标抬分。若 B0 已解出绝大多数题，先检查任务是否过于模板化；不能删掉规则基线。

最关键归因：
- B2≈B4：收益主要来自新数据，不能宣称联合算法有效。
- B3≈B4：证据链监督暂未体现额外价值。
- B5≥B6：不能假定 CoT 一定优于直接判断。
- 只在人工对照上提高、原生测试下降：任务或模板过拟合，不能宣称泛化。
- 安全提升但大量暂停：保守性增加，不能宣称安全效用同时改善。

## 7. 今晚执行顺序与预算

按约 6–8 小时工作窗规划，实际速度由设备、数据下载和 API 决定，不保证完成所有步骤。

P0（约 0–1 小时）：检查复用资产；数据适配和泄漏检查；40 组诊断/审阅队列；跑规则与已有模型小样本，确认问题是否存在。
P1（约 1–3 小时）：冻结划分；生成训练侧证据链；实现 B2/B3 的同数据比较。
P2（约 3–5 小时）：实现证据头及联合训练 B4；检查 loss 非零、梯度回传、分组无泄漏、证据有效性。
P3（约 5–8 小时）：完成同批比较、有限级联测试及错误分析；已有环境就做少量闭环，不为闭环临时搭建完整浏览器平台。

今晚无 GPU 时，仅运行已缓存编码器的冻结表征基线和 LLM 证据链概念验证，说明尚未完成表征微调；不能用 TF-IDF 的结果冒充 BERT 训练。没有 API 时保留小模型对比和数据审查，不伪造教师证据。

默认禁止自动高成本无限探索。先估算一次请求 token，设置 max_calls=300、max_input_tokens_per_call=8000、max_output_tokens_per_call=800 的首轮硬上限；这只是资源上限，不是 API 费用承诺。本地推理也记录耗时。不得接入真实邮件发送、生产数据删除或外网数据外发。

## 8. 必须交付的文件

- RUN_STATUS.md：已完成、失败、未运行及失败原因。
- data_manifest.json：来源、版本/commit、实际数量、类别分布、划分、排除项、审核状态、历史暴露标记。
- predictions.jsonl：逐样本输入引用、预测、真实标签、路由、证据、耗时、token、错误码。
- metrics.json / results.csv：从原始预测重新计算，不手填数字。
- ablation.csv：B2/B3/B4 的同数据对照，不存在结果留空并注明未运行。
- cases.md：正常放行、真实越权、证据不足、模型失败各至少一个（未找到者如实写未找到）。
- midterm_summary.md：只包含已运行结论、规模、限制与下一步；严格区分设计、单元测试、静态分类、闭环执行。

不需要反复计算哈希、建立多层签收系统或写大量防御性样板。保留 commit、固定随机种子和清晰原始日志即可。

## 9. 中期材料衔接

前期：BindGuard 授权执行原型与失败分析；本轮：关键上下文问题凝练、对照样本协议和协同训练方案；完成实验后：用日志填入本轮真实进度。不能把 BindGuard 的结果改名成为新方法结果，也不倒填实验月份。

论文建议结构：绪论；相关工作与问题定义；安全关键上下文对照数据与评测；对照语义学习方法；证据链协同检测与系统；实验与局限；总结。

## 10. 最近邻与创新边界

[1] ShieldAgent, ICML 2025，`https://arxiv.org/abs/2503.22738`：可验证策略、动作规则电路与工具验证。不能声称本工作首次把策略推理用于智能体护栏。
[2] CONSCENDI, NAACL 2024，`https://aclanthology.org/2024.naacl-long.223/`：场景引导、正反对照样本与小模型蒸馏。不能把“生成正反样本训练小模型”独立列为新颖性。
[3] PIGuard, ACL 2025，`https://aclanthology.org/2025.acl-long.1468/`：过度防御与触发词偏置。本文要覆盖真实任务授权关系而不只触发词。
[4] Task Shield, ACL 2025，`https://arxiv.org/abs/2412.16682`：任务对齐核验。不能将“判断是否服务于用户目标”作为首次提出。
[5] ToolSafe, Findings ACL 2026，`https://aclanthology.org/2026.findings-acl.1850/`：步骤级风险、行动与攻击关联、解释反馈与多任务训练。它是直接方法/数据近邻。
[6] AttriGuard, arXiv 2026，`https://arxiv.org/abs/2603.10749`：运行时反事实重执行和调用归因。本文离线干预可信上下文训练检测边界，不做其运行时重放归因，也不宣称反事实思想首次用于护栏。
[7] ARBITER, arXiv 2026，`https://arxiv.org/abs/2607.17575`：安全/不安全双假设推理、结构化损失和证据解释。本文不能把“双向思考并列出证据”单独称为创新。
[8] Calibrated Deferral Routing for Cost-Efficient Guardrails, ICML 2026 AIWILD workshop，`https://openreview.net/forum?id=veeRs1cBq9`：校准的不确定性级联。网页题录与摘要已核对，全文访问受浏览器验证影响，未据此作细节差异断言。
[9] AgentDojo, 2024，`https://arxiv.org/abs/2406.13352`：动态工具环境与攻击防御评测。
[10] Turpin et al., 2023，`https://arxiv.org/abs/2305.04388`：CoT 解释未必反映实际决策依据，证据引用也不是安全证明。

**最终贡献主张只能落在经实验验证的增量上：同一待执行指令下的关键上下文变化监督，与可追溯证据链监督共同改善条件化风险表征；相对相同数据的普通微调，降低近义但授权不同样本的混淆，并在级联中保留这一收益。**
