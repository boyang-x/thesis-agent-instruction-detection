# 毕设执行与结果同步约定

论文题目逐字保持：**基于语义表征与思维链推理协同的智能体恶意指令检测方法**。

用户于2026-09-24明确要求建立公开毕设仓库，并在每次实验完成后把必要结果推送到GitHub，供GPT Pro审阅和指导下一步。这是持续有效的项目内授权，不需要每轮重复询问是否推送。

## 仓库职责

- 本仓库：`boyang-x/thesis-agent-instruction-detection`，公开进度、必要代码快照和可重算结果。
- 实验继续复用原有 `agent-guardrail-research` 仓库及执行前入口，不为同步结果重写主入口。
- 初始任务书是 `Codex_毕设实施与实验任务书.md`；第二轮执行依据 `reviews/2026-09-24-round2-instructions.md` 及用户明确指令。保留第一轮负结果，优先原生数据/输入修复、普通基线和同模型同门控协同；未运行项不填成绩。
- 服务器教程、内网地址、凭据、权重、整份第三方轨迹留在本地。发布使用明确文件清单；不能 `git add .` 把无关文件一并提交。

## 每轮实验的完成条件

1. 收齐实际运行的原始预测、配置、状态、失败案例及资源用量。失败/中断也应同步；未运行不得填入数字。
2. 从原始预测重算指标，保留整数分母、abstain、数据集/种子/分组及证据等级。不得把silver、静态分类或历史结果升级为独立gold、闭环或新方法成绩。
3. 在本仓库执行 `scripts/publish_experiment.py --source <本轮产物目录> --run-id <唯一标识> --push`。导出后运行 `scripts/verify_run.py runs/<标识>`；未来格式变化时扩展适配器，不能绕过验证。
4. 更新 `CURRENT_STATUS.md`、`EXPERIMENTS.md` 及 `GPT_PRO_REVIEW.md`。明确已完成、阻塞、失败、未运行、主要结论和待GPT Pro审阅的问题。
5. 推送前fetch并检查远端差异；不force-push，不覆盖他人改动，不混入既有暂存内容。推送后核对远端HEAD。
6. 最终回复提供GitHub链接和同步提交。若鉴权、网络或审阅阻塞，只记录具体失败，不能声称已同步。

## GPT Pro反馈衔接

阅读 `GPT_PRO_REVIEW.md`。用户转回的审阅意见保存在 `reviews/`，区分建议、已采纳决定、未采纳理由和实际执行结果。审阅建议不是已运行证据，也不自动授权突破用户的RL、GUI、生产业务写入等边界。

本约定通过项目文件约束后续实验收尾；没有安装定时任务或假定无人值守监控。

2026-09-24补跑阶段约定：冻结`732834f3`的数据/检查点/输入/提示/门控，仅补HTTP402缺失，复用所有成功响应；新增200次/预计10元上限。完成补跑、原4例串联验证和发布后本阶段结束，无论有无增益，不自行设计或启动新算法轮次。

2026-09-24第三轮由用户另行授权：旧缓存机制消融、固定banking/travel迁移、预选最多64条同信息输入对照；检查点/输入/门控/提示不变，新请求统一2048，与旧800分表，响应跨种子和预算复用。第三轮完整清单取代上一补跑阶段的200次请求限制，保留402即停。用户随后收窄材料范围：本地只做实验和网页GPT Pro可用证据包，不生成中期报告/PPT。交付并推送后本轮结束，不自行追加算法。

2026-09-24第四轮由用户另行授权，基准`505c7ac6933bb1eaf8de9e4dbdbd1ade7c58a0dc`：安全性缓存分析；完整源域dev158固定95%召回/5%误报共同工作点选择；原64条仅补relation_short；12条真实小模型/门控/必要LLM的执行前接口与记录式派发。所有提示/检查点/阈值不变；无合格点不放宽、不看新域选点，接口回退旧主10%。四项完成即结束，不新增架构、训练、领域或写作任务。

2026-09-24缓存收尾由用户另行授权，基准`b7e8e9e`：保留原共同无合格点；新增各方案分别要求三种子在源dev158满足相同95%召回/5%误报的探索选点，另报FP+安全暂缓成本。仅复用旧/新域原始预测和路由，保留同门控对照、全部种子与三点；新域已暴露，不称独立确认。核对8—12条既有案例，AI审核不替代人工gold、不改官方标签。缺失才补请求，本次未缺失因此零新增。交付与推送后结束，不自动启动下一轮，不生成PPT/报告/讲稿。
