# 第二轮简短执行计划

run-id：`2026-09-24-round2-native-collaboration`。2026-09-24 11:38 UTC前建档。
论文题目：**基于语义表征与思维链推理协同的智能体恶意指令检测方法**。

1. 使用固定ToolSafe提交的Slack/workspace，恢复完整current_action，去掉第一轮自定P1/P2/P3。按规范化原始任务、同轨迹和完全相同输入连通分组；70/15/15本地探索划分，保留历史暴露标记。
2. 缓存LLMLingua-BERT作明确命名的工程基线；统一field-aware512，保留任务、完整候选参数及相关工具说明，溢出计abstain；head512只做seed42输入消融。
3. 先32条train学习链路检查；合格后跑R0原样规则、R1 TF-IDF线性、E普通CE种子42/43/44。训练输出每epoch train/dev指标，dev macro-F1选checkpoint，test仅最终预测。
4. 预测前固定最多32条dev和96条test配对子集。相同模型/上下文/800输出上限比较直接判断与关系核对；预留旧构造排错8次、主比较最多256次、真实router最多8次、探测1次和有限网络重试，总上限300。
5. 每个E种子由dev冻结10/25/50%门控；对同一test样本、同一门控比较两种缓存复算级联，并报告纠错、改错、未路由错误。另做真实router小样本，不把缓存时延当实测。
6. 打包12条待人工审核案例，继续官方标签探索；P3证据训练本轮默认未运行。输出两张主表、图数据、三页讲稿，更新公开审阅入口并推送。

预算：已有server-b单卡4090；新增GPU容器累计约2小时上限；不下载多个模型、不加购、不调用陌生服务。第一轮目录只读保留，当前用户指令是本轮多阶段试验授权。
